import { emit } from 'cluster';
import { EventDispatcher } from '../../utils/EventDispatcher';

export class BaseCtn extends PIXI.Container {
    // _evt:EventDispatcher
    constructor() {
        super()
        // this._evt = new EventDispatcher
    }
    // emit() {
        
    // }
    // emit = this._evt.emit

}